[project]
name = "markdfetch"
version = "0.1.0"
description = "Fetch web pages and convert them to markdown"
readme = "README.md"
requires-python = ">=3.10"

authors = [
    {name = "Aman Singh"}
]

license = "MIT"

dependencies = [
    "requests",
    "beautifulsoup4",
    "html2text"
]